name        = "conventional-commits"
description = "Conventional Commits enforcement: `committed` config + commit-msg lefthook hook + a CI workflow that lints a PR's non-merge commits."

provides       = ["commit-style"]
requires       = ["git-hooks"]
conflicts-with = []
